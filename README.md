# Place Anywhere Core - Fabric 1.21.1

## 中文说明

这是 **Place Anywhere Core** 模组的 Fabric 1.21.1 版本源码。

### 构建

```bash
./gradlew build
```

构建产物位于 `build/libs/` 目录。

## English

This is the source code for **Place Anywhere Core** mod, Fabric 1.21.1 version.

### Build

```bash
./gradlew build
```

The build output is located in `build/libs/`.
